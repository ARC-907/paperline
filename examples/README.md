# Examples

Sample artifacts showing what populated folders look like.

- `example-message-folder/` — what a populated `correspondence/<date>/<msg>/` folder looks like, including a document-pointer markdown
- (more examples can be added here as the kit grows)

Use these as references when populating your own project's content.
