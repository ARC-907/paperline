# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Obsidian workspace state (per-machine, not corpus content)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/graph.json

# Temp
*.tmp
*.swp
*.bak
*~

# DB rollback / WAL files (the .sqlite is committed, but SQLite working files are not)
system/corpus.sqlite-wal
system/corpus.sqlite-shm

# Runtime journal artifacts (regenerated by build_journal.py)
system/journal.sqlite
system/journal.sqlite-wal
system/journal.sqlite-shm
system/journal-operations.log

productization-plan/

# Operator-only planning notes (proposals, audits) — not for shipping
internal-planning/

# === Cruft prevention (added 2026-05-27 by portfolio sweep) ===
# These patterns are blocked by default because operator-side tooling, IDE
# config, planning material, and superseded code repeatedly leaked into
# shipping repos. Provenance of removed-superseded content lives in the
# ARC-907/products-cloud-archive bundles, NOT in product repos.

# Operator-side / internal planning

# IDE / editor config

# Cloudflare wrangler cache (contains operator email + account ID)
.wrangler/

# Superseded / cold-storage
archive/
legacy/

# Operator dev tooling
tools/
pre-publish-grep.ps1
pre-publish-grep.sh
audit-fix.ps1
audit-fix.sh

# OS junk
